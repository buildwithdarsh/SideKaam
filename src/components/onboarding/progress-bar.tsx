"use client";

// Progress bar is now inline in the onboarding page.
// This file kept as a no-op to avoid breaking any lingering imports.
export function ProgressBar({ current }: { current: number }) {
  return null;
}
